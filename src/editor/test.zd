main() {
    int a = 0;
    int tentativa;

    while (a < 5) {
        print("Solicitando input...");
        input tentativa;
        print("Tentativa: " + tentativa);

        if (tentativa < 3) {
            print("numero é maior!");
        }

        else if (tentativa == 3) {
            return;
        }

        else if (tentativa > 3) {
            print("O número é menor.");
        }
        else if(a == 5) {
        print("voce perdeu");
        return;
        }
        a ++;
    }
    print("Fim do loop");
    print("não era maior");
}
